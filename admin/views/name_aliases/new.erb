<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:name_aliases, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:name_aliases, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:name_alias) %></h2>
    <div class="inner">
      <% form_for :name_alias, url(:name_aliases, :create), :class => :form do |f| %>
        <%= partial 'name_aliases/form', :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial('base/sidebar') %>
